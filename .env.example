# The chat sidebar on the reader page streams through /api/chat, which calls
# OpenAI server-side. Without this the route returns 500 and the sidebar shows
# the error; the rest of Mimir (and the WebMCP tools) works without it.
OPENAI_API_KEY=
